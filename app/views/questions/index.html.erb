<script type="text/javascript">
  $(function () {
    var questionsData = <%= Question.all.to_json.html_safe %>

    var $content = $('.content');
    var $navbar = $('.main-nav');

    SU.initialize(
      $content,
      $navbar,
      questionsData
    );
  });
</script>
