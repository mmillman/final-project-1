<div class="comment">
  <%= comment.escape("body") %>
  -- <%= comment.escape("commenter_id") %>
  (<span title="<%= comment.escape("created_at") %>">
    <%= $.timeago(comment.escape("created_at")) %>
  </span>)
</div>